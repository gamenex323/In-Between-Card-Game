using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class FriendReq : MonoBehaviour
{
    public Text SearchFriendName;
    public Button sendReqBtn;
    // Start is called before the first frame update
    void Start()
    {
        //sendReqBtn.onClick.AddListener(() => {

        //    FriendsManager.Instance.CallUserFriendReq(DataManager.Instance.FindFriendsdata.search.id);
        //});
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
